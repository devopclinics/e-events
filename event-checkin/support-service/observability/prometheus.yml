global:
  scrape_interval: 15s
rule_files:
  - /etc/prometheus/support-alerts.yml
alerting:
  alertmanagers:
    - static_configs:
        - targets: ["support-alertmanager:9093"]
scrape_configs:
  - job_name: support-service
    static_configs:
      - targets: ["support-service:8002"]
