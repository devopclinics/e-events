node_modules/
dist/

# Capacitor native projects are generated by the build (mobile/build-android.sh).
# Commit them later if you want to customize native code; ignored by default.
android/
ios/

# Signing material — NEVER commit
*.jks
*.keystore
keystore.properties
