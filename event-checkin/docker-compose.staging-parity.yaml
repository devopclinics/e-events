# Optional overlay for manual staging Compose commands. Image versions continue
# to come from APP_VERSION and docker-compose.prod.yaml, keeping every service
# on the same release.
services:
  backend:
    environment:
      PUBLIC_SITE_MANAGEMENT_ENABLED: "true"
  public-site-service:
    environment:
      ENABLED: "true"
