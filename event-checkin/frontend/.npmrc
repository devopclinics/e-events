# @capacitor-firebase/authentication declares an OPTIONAL peer of firebase@^11,
# but the app runs firebase@^10. Our code uses firebase 10's signInWithCredential
# (stable API), not the plugin's optional firebase integration, so relax npm's
# strict peer resolution instead of force-upgrading the live app's Firebase SDK.
legacy-peer-deps=true
